/* config.js — App configuration
   Set RESULT_POST_URL to your GAS Web App URL to enable result submission.
   Set RESULT_POST_TOKEN to match the token configured in your GAS script.
*/
window.APP_CONFIG = {
  RESULT_POST_URL:   '',   // e.g. "https://script.google.com/macros/s/xxx/exec"
  RESULT_POST_TOKEN: '',   // must match AUTH_TOKEN in gas_result_receiver.js
};
